# jsonValidator
This project is a simple JSON validator web application created to demonstrate modern build tool usage with Webpack and npm. It provides a UI to validate a JSON data object against a JSON schema, reporting success or failure.
